<?xml version="1.0" encoding="utf-8"?>
<!--
    Phone Channel screen. Toolbar with back + channel title, then a vertical list of
    horizontally-scrolling shelves (one per VideoGroup the presenter emits).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/mobile_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/mobile_primary">

        <TextView
            android:id="@+id/btn_back"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:gravity="center"
            android:text="←"
            android:textColor="@color/mobile_text_primary"
            android:textSize="24sp"
            android:background="?android:attr/selectableItemBackground" />

        <TextView
            android:id="@+id/channel_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:singleLine="true"
            android:ellipsize="end"
            android:textColor="@color/mobile_text_primary"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/btn_subscribe"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:background="@drawable/bg_subscribe_pill"
            android:foreground="?android:attr/selectableItemBackground"
            android:singleLine="true"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/brand_accent_on"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/btn_bell"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="8dp"
            android:padding="8dp"
            android:scaleType="fitCenter"
            android:src="@drawable/settings_notifications"
            app:tint="@color/mobile_text_primary"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/notification_bell"
            android:visibility="gone" />
    </LinearLayout>

    <!--
        Channel identity header: circular avatar + subscriber count. Hidden until the first
        upload's metadata resolves (see MobileChannelFragment.maybeResolveSubscribedState).
    -->
    <LinearLayout
        android:id="@+id/channel_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:visibility="gone">

        <ImageView
            android:id="@+id/channel_avatar"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@drawable/avatar_circle_bg"
            android:scaleType="centerCrop"
            android:contentDescription="@string/channel_avatar" />

        <TextView
            android:id="@+id/channel_subs"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:textColor="@color/mobile_text_secondary"
            android:textSize="15sp" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipe_refresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/channel_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingBottom="16dp" />
        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />
    </FrameLayout>
</LinearLayout>
