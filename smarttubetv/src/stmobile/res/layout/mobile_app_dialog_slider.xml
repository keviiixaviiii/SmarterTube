<?xml version="1.0" encoding="utf-8"?>
<!-- Numeric single-choice row: a radio category whose options form an ordered numeric range
     (speed, zoom, seek interval, auto-hide timeout) rendered as a discrete slider over the
     option list, with the current value shown on the right. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="56dp"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/slider_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/mobile_text_primary"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/slider_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:textColor="@color/mobile_accent"
            android:textSize="15sp" />
    </LinearLayout>

    <com.liskovsoft.smartyoutubetv2.mobile.ui.dialogs.ScrollSafeSeekBar
        android:id="@+id/slider_seek"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:progressTint="@color/mobile_accent"
        android:progressBackgroundTint="@color/mobile_text_secondary"
        android:thumbTint="@color/mobile_accent" />
</LinearLayout>
